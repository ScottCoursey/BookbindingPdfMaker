namespace BookbindingPdfMaker.Models
{
    internal enum BookSize
    {
        StandardPaperback,      // 120mm x 180mm page size
        LargeFormatPaperback,   // 150mm x 205mm page size
        FullPaperSize,          // Determined by printer
        Custom                  // User specified
    }
}
